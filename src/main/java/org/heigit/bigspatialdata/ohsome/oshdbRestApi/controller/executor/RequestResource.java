package org.heigit.bigspatialdata.ohsome.oshdbRestApi.controller.executor;

/** Enumeration defining the request resource (LENGTH, PERIMETER, AREA) */
public enum RequestResource {
  LENGTH, PERIMETER, AREA
}
